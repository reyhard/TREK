# Upstream 3.4.1 Upgrade Report

## Source refs
- Fork baseline (3.4.0 sync): `a3ff9b45`
- Upstream functional release: `91095ef9`
- Upstream version target: `a0994658`
- Merge commit: `aa364b3f` (parents: `a3ff9b45` + `a0994658`)
- Formatting follow-up: `8a718b2e` (style(sync): format upstream 3.4.1 changes)
- Current tip: `8a718b2e` on `integration/upstream-3.4.1`

## Task commits
- `c6d8090d` — docs(sync): record upstream 3.4.1 verification
- `60f38a89` — docs(progress): record Task 09 deployment docs normalization completion
- `4a8a54ce` — docs(deploy): normalize TREK image and chart references
- `7975d992` — fix(plugins): prefer resolved registry screenshot over legacy guess
- `8e9ab641` — fix(memories): normalize Synology skip-SSL, add TLS coverage, fix placeholder set equality, add arrayBuffer to thumbnail fixture
- `e519a3f9` — fix(memories): honor Synology streaming TLS settings
- `45f02b12` — fix(atlas): include Kosovo ADM1 regions in regenerated bundle
- `f2482d31` — fix(notifications): remove admin token fallback from test-ntfy controller endpoint
- `c8950368` — fix(notifications): remove admin token fallback from per-user ntfy sends
- `151d5934` — fix(notifications): isolate per-user ntfy topics
- `315e6cb5` — fix(planner): require timed post-check-in morning stop
- `e95b9897` — fix(reservations): keep AirTrail modal hook order stable
- `482bc3b5` — fix(transit): lead arrive-by results with latest arrival
- `beea878c` — fix(task01): correct merge SHA, assign conflict owners, add baseline evidence
- `8a718b2e` — style(sync): format upstream 3.4.1 changes (prettier, 13 files)

## Focused verification

### Step 1: Release-blocking tests
| Suite | Result | Tests |
|-------|--------|-------|
| Client (transit, AirTrail, dayOrder, dayMovement, routeCalc, atlasModel, JourneyDetail) | PASS | 247 passed, 38 skipped |
| Server (notification, atlasBundle, memories, plugins registry) | PASS | 224 passed |
| Shared (i18n placeholders) | PASS | 1 passed |

### Step 2: Preserved-fork regression suites
| Suite | Result | Tests |
|-------|--------|-------|
| Server (tools-transit, plugins unit + integration) | PASS | 740 passed (49 files) |
| Client (MapView, MapViewGL, DayPlanSidebar, movementStats, connectionsVisibility) | PASS | 199 passed (4 files) |

### Step 3: Static checks and builds
| Check | Result | Detail |
|-------|--------|--------|
| Server typecheck | PASS | exit 0 |
| Client typecheck | PASS | exit 0 |
| Server lint | PASS | 0 errors, 1994 warnings (pre-existing) |
| Client lint | PASS | 0 errors, 1249 warnings (pre-existing) |
| Server format | PASS | (10 files fixed by prettier) |
| Client format | PASS | (3 files fixed by prettier) |
| Server build | PASS | dist ready |
| Client build | PASS | built in 14.97s |
| Plugin-sdk build | FAIL | pre-existing — missing `@clack/prompts`, `update-notifier` type declarations |

### Step 4: Full-suite comparison to baseline
| Suite | Files | Pass | Fail | Skip | Tests Passed |
|-------|-------|------|------|------|-------------|
| Shared | 34 | 34 | 0 | 0 | 140 |
| Server | 304 | 302 | 2 | 0 | 5473 |
| Client | 212 | 209 | 3 | 38 | 3619 |
| Plugin-sdk | 12 | 6 | 6 | 0 | 106 |

#### Server failures (2, both pre-existing):
1. `tests/e2e/oauth.e2e.test.ts` — E2E test requires running server (environment limitation)
2. `tests/integration/upstream-3.4-migration.test.ts` — Missing Task 03 fork fixture (documented baseline gap)

#### Client failures (24, all pre-existing):
- `tests/e2e/*` — 19 E2E test files require running server (environment limitation)
- `PlaceInspector.test.tsx` — 3 tests (FE-PLANNER-INSPECTOR-037, -044, -045)
- `TransitJourneyModal.test.tsx` — 2 tests (FE-PLANNER-TRANSITJOURNEY-002, -006)

#### Plugin-sdk failures (6 test files, pre-existing):
- Missing `@clack/prompts` and `update-notifier` packages

No new regressions introduced by the 3.4.1 merge. All observed failures match the documented Task 01 baseline.

### Step 5: Repository state
| Check | Result |
|-------|--------|
| No conflict markers | PASS (only `=======` in CSS comments, diffs, binary files) |
| No focused tests (`.only`) | PASS |
| Clean whitespace (`git diff --check`) | PASS |
| Upstream target is ancestor | PASS (`a0994658` is an ancestor of HEAD) |
| Git worktree | Clean — format-only changes committed as `8a718b2e` |

### Step 6: Deployment checks
| Check | Result |
|-------|--------|
| `docker compose config` | PASS — parses cleanly |
| `docker build` | BLOCKED — permission denied on Docker socket |
| `helm lint` | BLOCKED — helm not installed |

## Environment blockers (unresolved)
The following are **not** resolved in this workspace. They remain as pre-existing environmental limitations that must be addressed before any release:

- **Docker daemon access**: not available (permission denied) — Docker build cannot be verified
- **Helm CLI**: not installed — Helm chart lint and template cannot be verified
- **Plugin-sdk build**: blocked by missing `@clack/prompts` and `update-notifier` dev dependencies
- **Staging smoke tests**: not executed — no staging environment with 3.4.0 data available
- **Task 03 fork fixture**: absent from workspace — `pre-upstream-3.4-fork.sqlite` is required for the `upstream-3.4-migration.test.ts` integration test

**This worktree is not release-ready.** All four environmental blockers (Docker, Helm, SDK deps, staging) plus the missing fixture must be resolved before a release can be cut.

## Intentional behavior changes
- Transit arrive-by result ordering (Task 02): stable sort preserves provider order for equal arrival times
- Check-in-day morning-leg suppression (Task 04): untimed first stop on check-in day suppresses hotel-top route
- Per-user ntfy topic isolation (Task 05): whitespace-only topics treated as unset; one user's topic never reused for another
- Registry screenshot resolution (Task 08): resolved screenshot URL preferred over legacy guess
- Synology skip-SSL/TLS coverage (Task 07): TLS settings forwarded to image fetches
- Deployment reference normalization (Task 09): lowercase Docker images, canonical chart URLs, chart version 3.4.1

## Database impact
No schema migrations in upstream 3.4.1. The fork's migration chain (176 migrations) was preserved through the merge. The pre-upstream-3.4-fork.sqlite fixture is absent from this workspace (Task 03 responsibility).

## Rollback
Stop 3.4.1 and redeploy the previous 3.4.0 fork image. Restore data only if an unrelated runtime operation changed it; the release itself adds no migration. Rollback is application-image rollback.
